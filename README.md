# Float Int Tool

A static IEEE-754 floating-point explorer for GitHub Pages.

It supports:

- float16, float32, float64, and x87-style float80 bit layouts
- bit toggling
- hexadecimal editing
- decimal input and preset values

Open `index.html` directly in a browser, or publish the repository with GitHub Pages.
